spring:
    datasource:
        url: jdbc:h2:mem:testdb;MODE=MYSQL;DB_CLOSE_DELAY=-1
logging:
    level:
        root: info
        java:
            sql: debug
        com:
            devp: trace
devp:
    devMode: 1
    dev-project-dir: /home/devp/test
    security-enabled: false
